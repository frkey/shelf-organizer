class Motor {
  public:
    void addItem(String color, int line, int column);
    void removeItem(String color, int line, int column);
} motor;

void Motor::addItem(String color, int line, int column) {
  if(color.equalsIgnoreCase("RED")) {
    // TODO Implementar aqui as movimentações para a prateleira vermelha
  } else if(color.equalsIgnoreCase("GREEN")) {
    // TODO Implementar aqui as movimentações para a prateleira verde
  } else if(color.equalsIgnoreCase("BLUE")) {
    // TODO Implementar aqui as movimentações para a prateleira azul
  }
}

void Motor::removeItem(String color, int line, int column) {
  if(color.equalsIgnoreCase("RED")) {
    // TODO Implementar aqui as movimentações para a prateleira vermelha
  } else if(color.equalsIgnoreCase("GREEN")) {
    // TODO Implementar aqui as movimentações para a prateleira verde
  } else if(color.equalsIgnoreCase("BLUE")) {
    // TODO Implementar aqui as movimentações para a prateleira azul
  }
}

